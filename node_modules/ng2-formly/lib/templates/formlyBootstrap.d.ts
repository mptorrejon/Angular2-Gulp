import { FormlyConfig } from "../services/formly.config";
import { FormlyMessages } from "../services/formly.messages";
export declare class FormlyBootstrap {
    constructor(fc: FormlyConfig, fm: FormlyMessages);
}
